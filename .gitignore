# OS
.DS_Store
Thumbs.db

# Non-code documents that land in this folder (this repo is public) —
# business/financial/legal docs have no reason to be in git history.
*.docx
*.doc
*.xlsx
*.pdf

# Raw original upload (4K/60fps/54Mbps, ~309MB) — kept on disk as the
# source master but not tracked in git; public/assets/hero-video.mp4 is the
# web-optimized 1080p/30fps encode actually served on the site.
public/assets/video-hero.MP4
public/assets/hero-video-web.mp4

# Next.js
.next/
.next_stale_*/
next-env.d.ts
tsconfig.tsbuildinfo

# Auto-regenerated by `next dev` on every run (Next.js 16 agentRules feature)
AGENTS.md
CLAUDE.md

# Env vars — real Supabase/Stripe keys live in .env.local, never committed.
# .env.local.example documents the required vars with placeholder values.
.env
.env.local
.env.*.local

# Editor
.vscode/

# Node / CI harness (Phase 0 — §49.1)
node_modules/
playwright-report/
playwright/.auth/
test-results/
.lighthouseci/
